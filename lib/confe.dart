import 'package:flutter/material.dart';

ColorScheme domainScheme = ColorScheme.fromSeed(
  brightness: Brightness.dark,
  seedColor: Color(0xffa85513)
);

ColorScheme methodScheme = ColorScheme.fromSeed(
  brightness: Brightness.dark,
  seedColor: Color(0xff7ADE2D)
);
ColorScheme eventScheme = ColorScheme.fromSeed(
  brightness: Brightness.dark,
  seedColor: Color(0xffFF8AFF)
);
ColorScheme typeScheme = ColorScheme.fromSeed(
  brightness: Brightness.dark,
  seedColor: Color(0xff24E5E4)
);

ColorScheme mainScheme = ColorScheme.fromSeed(
  brightness: Brightness.dark,
  seedColor: Color(0xff287DFF)
);